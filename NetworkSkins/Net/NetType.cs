namespace NetworkSkins.Net
{
    public enum NetType
    {
        // NetUtil.NET_TYPE_NAMES must always be updated when this is modified!
        Undefined = -1,
        Tunnel = 0,
        Ground = 1,
        Elevated = 2,
        Bridge = 3
    }
}
